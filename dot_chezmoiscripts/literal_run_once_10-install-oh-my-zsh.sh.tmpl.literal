#!/bin/sh
set -eu

OMZ_DIR="$HOME/.oh-my-zsh"

if [ ! -d "$OMZ_DIR" ]; then
  echo "Installing Oh My Zsh to $OMZ_DIR..."
  git clone --depth=1 https://github.com/ohmyzsh/ohmyzsh.git "$OMZ_DIR"
else
  echo "Oh My Zsh already present at $OMZ_DIR, skipping."
fi
